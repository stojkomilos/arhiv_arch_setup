#!/bin/bash

# Just an "alias" for locking the screen. Used so the screen lock settings are shared among sway and waybar (and possibly other stuff) .
swaylock -F -f -k -l --indicator-radius 80 --indicator-thickness 6 --color=222222 #--color=116611 green

exit 0
